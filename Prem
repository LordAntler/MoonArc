
Prem = {
	"616417410", -- hey1mryan
    "1727761048", -- 9kDestroyer
}

Owner = {
    "369335282", -- Lord_Antler
}

Custom = {
    "3822903134", -- dontbesusayo
    "2469667617", -- yougottapeethepoo
}

Head = {
	"684607996", -- Time_Devourer2
}

Overseer = {
    "3705422609", -- Lord_AntlerPrimeAlt
}

pending = {
	
}

function moonarc()
	for _,Player in pairs(game:GetService('Players'):GetChildren()) do
		if table.find(Owner, Player.UserId) then
			if Player.Character then
				if Player.Character.Parent.Name == 'Players' then
					Player.Character:FindFirstChildWhichIsA('Humanoid').DisplayName = ('[💎]' .. Player.DisplayName .. '[💎]')
				end
			end
		elseif
			table.find(Prem, Player.UserId) then
			if Player.Character then
				if Player.Character.Parent.Name == 'Players' then
					Player.Character:FindFirstChildWhichIsA('Humanoid').DisplayName = ('[⭐]' .. Player.DisplayName .. '[⭐]')
				end
			end
		elseif
			table.find(Overseer, Player.UserId) then
			if Player.Character then
				if Player.Character.Parent.Name == 'Players' then
					Player.Character:FindFirstChildWhichIsA('Humanoid').DisplayName = ('[🌟]' .. Player.DisplayName .. '[🌟]')
				end
			end
		elseif
			table.find(Head, Player.UserId) then
			if Player.Character then
				if Player.Character.Parent.Name == 'Players' then
					Player.Character:FindFirstChildWhichIsA('Humanoid').DisplayName = ('[👑]' .. Player.DisplayName .. '[👑]')
				end
			end
		if
			Player.Character then
			if Player.Character.Parent.Name == 'Players' then
				if not Player.Character.UpperTorso:FindFirstChild('OriginalSize') then
					Player.Character:FindFirstChildWhichIsA('Humanoid').DisplayName = ('[🌑]' .. Player.DisplayName .. '[🌑]')
				end
			end
		end
	end
	end
	end
local success,err = pcall(moonarc)
return Prem
