blklist = {
"hey1mryan",
}
function makethemblack()
for _,Player in ipairs(game:GetService("Players"):GetChildren()) do
if not Player.Character.Head:FindFirstChild("OriginalSize") then
if table.find(blklist, Player.Name) then
Player.Character.Humanoid.DisplayName = ("[黑鬼]" .. Player.DisplayName .. "[黑鬼]")
return blklist
